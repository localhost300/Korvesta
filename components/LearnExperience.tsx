"use client";
import { useState } from "react";
import Link from "next/link";
import { IconBook2,IconBuildingBank,IconChartPie,IconClock,IconSearch,IconShieldCheck } from "@tabler/icons-react";
import { lessons } from "@/lib/data";
export function LearnExperience(){const [query,setQuery]=useState("");const filtered=lessons.filter(l=>`${l.title} ${l.desc}`.toLowerCase().includes(query.toLowerCase()));return <div className="container-shell py-11"><section className="surface grid-bg p-8 sm:p-10"><h1 className="text-5xl font-semibold tracking-[-.05em]">Learn. Invest. <span className="text-[var(--amber)]">Grow.</span></h1><p className="mt-4 max-w-xl leading-7 text-muted">Build a durable understanding of Treasury securities, bond ETFs, income and risk.</p><label className="relative mt-6 block max-w-md"><IconSearch size={17} className="absolute left-4 top-1/2 -translate-y-1/2 text-muted"/><input value={query} onChange={e=>setQuery(e.target.value)} className="field" placeholder="Search bonds, yields, duration..."/></label></section><div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{[[IconBook2,"Bond Basics","Start with the essentials"],[IconBuildingBank,"Treasuries","Bills, notes and bonds"],[IconChartPie,"Bond ETFs","Diversified exposure"],[IconShieldCheck,"Risk Management","Protect capital"]].map(([Icon,t,c],i)=><button type="button" key={String(t)} onClick={()=>setQuery(i===0?"":String(t))} className="surface flex items-center gap-3 p-4 text-left hover:border-[var(--amber)]"><Icon size={22} className="text-[var(--amber)]"/><span><strong className="block text-xs">{String(t)}</strong><small className="text-muted">{String(c)}</small></span></button>)}</div><section id="guides" className="mt-10"><div className="mb-4 flex justify-between"><h2 className="text-xl font-semibold">Fixed-Income Guides</h2><button type="button" onClick={()=>setQuery("")} className="text-xs text-[var(--amber)]">View all guides →</button></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">{filtered.map(l=><article key={l.title} className="surface p-6"><IconBook2 size={34} className="text-[var(--amber)]"/><h3 className="mt-5 font-semibold">{l.title}</h3><p className="mt-2 min-h-12 text-xs leading-5 text-muted">{l.desc}</p><span className="mt-4 block text-[10px] text-[var(--amber)]">{l.level} · {l.time}</span><Link href="/support" className="ghost-button mt-5 min-h-9 text-xs">Open guide</Link></article>)}</div></section><section className="mt-10 grid gap-4 md:grid-cols-3">{[["Yield","The annualized return implied by a bond's price and cash flows."],["Duration","A measure of price sensitivity to interest-rate changes."],["Credit spread","The extra yield over a comparable government security."]].map(([t,c])=><article key={t} className="surface p-6"><h2 className="font-semibold">{t}</h2><p className="mt-2 text-sm leading-6 text-muted">{c}</p></article>)}</section><section className="surface mt-10 flex flex-col gap-5 p-7 sm:flex-row sm:items-center"><IconClock size={38} className="text-[var(--amber)]"/><div className="flex-1"><h2 className="font-semibold">Join a Treasury and bond ETF session</h2><p className="mt-1 text-xs text-muted">Ask practical questions about yield, maturity, duration and diversification.</p></div><a href="mailto:management@korvesta.com?subject=Fixed-Income%20Learning%20Session" className="gold-button">Register interest</a></section></div>}
