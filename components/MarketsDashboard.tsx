"use client";
import { useMemo,useState } from "react";
import { IconBell,IconSearch,IconStar } from "@tabler/icons-react";
import { assets } from "@/lib/data";
import { MarketStatCard,MarketsTable,MoversList } from "./MarketCards";
export function MarketsDashboard(){const [query,setQuery]=useState("");const visible=useMemo(()=>assets.filter(a=>`${a.name} ${a.symbol}`.toLowerCase().includes(query.toLowerCase())),[query]);return <div className="container-shell py-10"><div className="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><h1 className="text-4xl font-semibold tracking-[-.04em]">Bond Markets</h1><p className="mt-2 text-sm text-muted">Treasury yields, bond ETFs and fixed-income research</p></div><label className="relative"><IconSearch size={16} className="absolute left-3 top-1/2 -translate-y-1/2 text-muted"/><input value={query} onChange={e=>setQuery(e.target.value)} className="field pl-9" placeholder="Search bonds and ETFs..."/></label></div><div className="mt-7 grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><MarketStatCard label="10Y Treasury yield" value="4.28%" change={-.04} data={assets[0].data}/><MarketStatCard label="2Y Treasury yield" value="4.71%" change={-.02} data={assets[1].data}/><MarketStatCard label="10Y–2Y spread" value="-43 bps" change={.02} data={assets[3].data}/><MarketStatCard label="Investment-grade spread" value="92 bps" change={-.03} data={assets[6].data}/></div><div className="mt-4 grid gap-4 xl:grid-cols-[1fr_320px]"><section><div className="mb-3 flex gap-2">{["All fixed income","Treasuries","Bond ETFs","Corporate bonds"].map(t=><button key={t} type="button" onClick={()=>setQuery(t==="All fixed income"?"":t==="Treasuries"?"Treasury":t==="Bond ETFs"?"ETF":"Corporate")} className="ghost-button min-h-9 text-xs">{t}</button>)}</div>{visible.length?<MarketsTable limit={visible.length}/>:<div className="surface p-12 text-center text-sm text-muted">No securities match your search.</div>}</section><aside className="surface p-5"><h2 className="font-semibold">Active bond ETFs</h2><p className="mt-1 text-xs text-muted">Illustrative delayed market snapshot</p><div className="mt-5"><MoversList/></div><a href="mailto:management@korvesta.com?subject=Korvesta%20bond%20market%20alert" className="gold-button mt-6 w-full"><IconBell size={16}/> Request an alert</a><a href="/learn" className="ghost-button mt-2 w-full"><IconStar size={16}/> Learn the risks</a></aside></div></div>}
