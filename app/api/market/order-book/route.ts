import { NextResponse } from "next/server";
const allowed=new Set(["BTC","ETH","SOL","BNB","XRP"]);
export async function GET(request:Request){const symbol=(new URL(request.url).searchParams.get("symbol")??"BTC").toUpperCase();if(!allowed.has(symbol))return NextResponse.json({error:"Unsupported market."},{status:400});try{const response=await fetch(`https://data-api.binance.vision/api/v3/depth?symbol=${symbol}USDT&limit=20`,{cache:"no-store"});if(!response.ok)throw new Error();const data=await response.json();return NextResponse.json({bids:data.bids??[],asks:data.asks??[]})}catch{return NextResponse.json({error:"Order book is temporarily unavailable."},{status:502})}}
