import type { MetadataRoute } from "next";
export default function sitemap(): MetadataRoute.Sitemap { const origin = process.env.NEXT_PUBLIC_SITE_URL ?? "http://localhost:3000"; return ["", "/markets", "/insights", "/learn", "/trade-tools", "/company", "/support", "/legal/terms", "/legal/privacy", "/legal/cookies"].map((path) => ({ url: `${origin}${path}`, lastModified: new Date(), changeFrequency: path === "/markets" ? "daily" : "monthly" })); }
